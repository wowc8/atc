# ATC Environment Variables
# Copy to .env and fill in values

# Anthropic API key (optional — only if ATC makes direct API calls)
# ANTHROPIC_API_KEY=sk-ant-...

# GitHub token (optional — uses gh CLI auth by default)
# GITHUB_TOKEN=ghp_...

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the ATC desktop shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "process:default",
    "shell:allow-execute",
    "shell:allow-spawn"
  ]
}
